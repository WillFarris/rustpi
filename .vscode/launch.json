{
    "version": "0.2.0",
    "editor.rulers": [100],
    "configurations": [
        {
            "type": "gdb",
            "request": "attach",
            "name": "Attach to QEMU",
            "executable": "target/aarch64-unknown-none/release/kernel",
            "target": "localhost:1234",
            "remote": true,
            "stopAtEntry": false,
            "cwd": "${workspaceRoot}", 
            "gdbpath": "aarch64-linux-gnu-gdb",
            "autorun": [
            ]
        }
        
    ]
}